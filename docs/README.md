# Screenshots & demo assets

Drop the project's visual assets here and reference them from the root `README.md`.

| File                 | What to capture                                                                 |
| -------------------- | ------------------------------------------------------------------------------- |
| `demo.gif`           | One full ticket flow: **Process → timeline → approval → resolved**.             |
| `dashboard.png`      | The `/tickets` dashboard with a few tickets and the status/urgency filters.     |
| `ticket-detail.png`  | A `/tickets/{id}` page showing the agent-run timeline and the approval card.    |
| `provider-switch.png`| The header provider selector with the Ollama online indicator.                  |

All assets here are committed and referenced from the root `README.md`. `demo.gif` (640px, 10 fps,
~2.5 MB) renders inline; `demo.mp4` (1280px, ~1 MB) is the higher-quality version of the same
walkthrough. Both were compressed from a 1080p/100 fps source with ffmpeg — keep new recordings small
(GitHub rejects files over 100 MB and large GIFs render slowly).
